{
  "Redis": {
    "Url": "${REDIS_HOST}:${REDIS_PORT}",
    "StreamKey": "product.${DATABASE_NAME}.dbo.Product",
    "ConsumerGroup": "cdc-consumers",
    "ConsumerName": "consumer-1"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "CdcRedisConsumer": "Debug"
    }
  }
}
