## 1. 主线与边界

- [x] 1.1 将 `CdcHttpServer + CdcWebUIClient` 明确为推荐主线，并在文档中写明原因
- [x] 1.2 将 `CdcWebUI` 标记为归档候选或非推荐入口，不再作为默认启动方案

## 2. API 契约统一

- [x] 2.1 定义统一的 CDC 事件列表 DTO、事件详情 DTO、统计 DTO
- [x] 2.2 调整 `CdcHttpServer` 的 `/api/events`、`/api/events/{id}`、`/api/stats` 输出结构
- [x] 2.3 调整 `CdcWebUIClient` 的 `src/types/cdc.ts` 与接口消费逻辑，使其与后端契约一致
- [x] 2.4 校验前端筛选、列表展示、详情展示在统一契约下行为正常

## 3. 编码与文档

- [x] 3.1 修复当前关键源码与文档中的乱码
- [x] 3.2 建立“中文为主、UTF-8 统一”的仓库约定
- [x] 3.3 更新 README 与过程文档，只保留一套推荐入口

## 4. 验证

- [x] 4.1 验证推荐启动路径可运行
- [x] 4.2 验证前后端接口联调正常
- [x] 4.3 验证中文文档、终端输出与界面文案不再出现乱码
